# Minimal runtime dependency: the official Model Context Protocol Python SDK.
# (pyproject.toml is the primary packaging metadata; this file is provided
# for `pip install -r requirements.txt` convenience / non-editable installs.)
mcp>=1.6.0,<2
typing-extensions>=4.8
